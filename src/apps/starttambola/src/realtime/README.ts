// ─── Supabase Realtime broadcast helpers ─────────────────────────────────────
// Place realtime channel management and broadcast utilities here.
//
// Example helpers to implement:
//   broadcastNumberDrawn(gameId, number)  — push a drawn number to all ticket holders
//   broadcastGameStatus(gameId, status)   — notify when game starts/ends
//   broadcastWinner(gameId, prizeType, ticketId) — announce a winner
//
// Use the supabaseAdmin client from config/supabase.ts for server-side broadcasts.
