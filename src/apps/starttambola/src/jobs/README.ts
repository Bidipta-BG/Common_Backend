// ─── Background / Cron Jobs ───────────────────────────────────────────────────
// Place scheduled job files in this directory.
// Example: game-expiry.job.ts, ticket-cleanup.job.ts
//
// Convention:
//   export async function runXyzJob(): Promise<void> { ... }
//   Register the job in a central scheduler (e.g. node-cron) imported in server.ts
