// ─── Background / Cron Jobs ───────────────────────────────────────────────────
// Place scheduled job files in this directory.
//
// Convention: one file per job, e.g.:
//   gameExpiry.job.js  — automatically close games past their end time
//   ticketCleanup.job.js — purge abandoned unpaid tickets
//
// Example structure:
//   const cron = require('node-cron');
//   cron.schedule('*/5 * * * *', async () => { ... });

module.exports = {};
